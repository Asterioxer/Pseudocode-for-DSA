#include<bits/stdc++.h>
using namespace std;
int main() {

    return 0;
}

void unordered_maps() {
    // same as set the only difference lies in the uniqueness of keys but, the map would be unsorted
    //O(1)  sometimes, O(N);worst case
}