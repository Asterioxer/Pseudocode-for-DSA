#include<bits/stdc++.h>
using namespace std;
int main() {

    return 0;
}

void Unordered_set() {
    unordered_set<int> st;
    //lower_bound and upper_bound function
    //does not work, rest all functions are the same
    //as above, it does store in any particular order
    //it has better complexity than set in most cases, except some when collision happens
    //O(1) -> most cases //O(N) happens once is a blue moon //big O of linear time -> once in a millenium
}